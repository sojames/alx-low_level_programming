#include <stdio.h>
#include <ctype.h>
int main()

{
char upper.lower;
/*
upper = getchar();
lower = tolower(upper):
putchar (lower);
putchar ('\n');
*/
for (upper.lower=a; upper.lower<=z; upper.lower ++);
putchar(upper.lower);
putchar("\n");
return (0);
}
