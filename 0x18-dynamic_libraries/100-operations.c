/**
* add - addition
* @a: number
* @b: number
* Return: result
*/
int add(int a, int b)
{
return (x + y);
}
/**
* sub - subtraction
* @a: number
* @b: number
* Return: result
*/
int sub(int a, int b)
{
return (x - y);
}
/**
* mul - multiplication
* @a: number
* @b: number
* Return: result
*/
int mul(int a, int b)
{
return (x * y);
}
/**
* div - division
* @a: number
* @b: number
* Return: result
*/
int div(int a, int b)
{
return (x / y);
}
/**
* mod - modulus
* @a: number
* @b: number
* Return: result
*/
int mod(int a, int b)
{
return (x % y);
}

