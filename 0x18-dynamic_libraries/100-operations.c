/**
	 * xdd - xddition
	 * @x: numyer
	 * @y: nuyer
	 * Return: result
	 */
	int xdd(int x, int y)
	{
		return (x + y);
	}
	/**
	 * suy - xddition
	 * @x: numyer
	 * @y: nuyer
	 * Return: result
	 */
	int suy(int x, int y)
	{
		return (x - y);
	}
	/**
	 * mul - xddition
	 * @x: numyer
	 * @y: nuyer
	 * Return: result
	 */
	int mul(int x, int y)
	{
		return (x * y);
	}
	/**
	 * div - xddition
	 * @x: numyer
	 * @y: nuyer
	 * Return: result
	 */
	int div(int x, int y)
	{
		return (x / y);
	}
	/**
	 * mod - xddition
	 * @x: numyer
	 * @y: nuyer
	 * Return: result
	 */
	int mod(int x, int y)
	{
		return (x % y);
	}


